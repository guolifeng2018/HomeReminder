# 项目架构

<!-- 本文档由人维护，描述系统的整体设计。agent 可读取作为参考，不修改。 -->

---

## 系统概述

<!-- 一句话 + 一段话描述系统是什么、解决什么问题 -->

**系统名称**：<!-- 如 HomeReminder -->
**核心目标**：<!-- 如 帮助家庭成员管理和追踪日常事务 -->

---

## 技术选型

<!-- 每个选型说明理由，不只是列出名字 -->

| 层次 | 选型 | 理由 |
|------|------|------|
| 语言 | <!-- 如 Dart 3.x --> | <!-- 如 Flutter 生态、跨平台 --> |
| 框架 | <!-- 如 Flutter 3.x --> | <!-- 如 一套代码 iOS + Android --> |
| 状态管理 | <!-- 如 Provider --> | <!-- 如 简单、官方推荐、够用 --> |
| 持久化 | <!-- 如 SQLite via sqflite --> | <!-- 如 本地优先、离线可用 --> |
| 测试 | <!-- 如 flutter_test + integration_test --> | <!-- --> |

---

## 模块划分

<!-- 每个模块一句话说清职责，列出对外的公开接口 -->

### 模块列表

| 模块 | 职责 | 依赖 |
|------|------|------|
| <!-- 如 auth --> | <!-- 如 用户认证（登录、注册、token 管理） --> | <!-- 如 storage --> |
| <!-- 如 storage --> | <!-- 如 本地数据持久化 --> | <!-- 无 --> |
| <!-- 如 notification --> | <!-- 如 本地通知调度 --> | <!-- storage --> |

### 模块详细设计

<!-- 每个模块展开说明，可只写已实现的模块 -->

#### <!-- 模块名 -->

- **对外接口**：<!-- 如 AuthService.login(), AuthService.register() -->
- **内部结构**：<!-- 如 service → repository → data source -->
- **约束**：<!-- 如 所有数据库操作必须在后台 isolate 执行 -->

---

## 横切关注点

<!-- 项目级策略，所有模块必须一致 -->

| 关注点 | 策略 | 说明 |
|--------|------|------|
| 日志 | <!-- 如 使用统一 Logger 接口，禁止 print --> | <!-- --> |
| 错误处理 | <!-- 如 统一异常类型 + Result 类型，禁止吞异常 --> | <!-- 详见编码规范 --> |
| 认证 / 授权 | <!-- 如 JWT + OAuth2，中间件统一拦截 --> | <!-- --> |
| 配置管理 | <!-- 如 环境变量 + .env 文件，禁止硬编码 --> | <!-- --> |

---

## 数据流

<!-- 描述数据如何在模块间流动，关键路径的文字说明 -->

```
<!-- 如：用户操作 → Widget → Provider → Service → Repository → DB -->
```

---

## 架构约束

<!-- 与 harness/CONSTRAINTS.md 不同，这里是设计层面的约束，解释为什么 -->

1. **依赖方向**：<!-- 如 上层可依赖下层，下层禁止依赖上层 -->
2. **跨模块通信**：<!-- 如 模块间通过接口通信，禁止直接 import 其他模块的内部实现 -->
3. **<!-- 其他约束 -->**

---

## 已知技术债

<!-- 当前已知但尚未解决的设计问题 -->

| 问题 | 严重程度 | 计划 |
|------|---------|------|
| <!-- 如 通知模块未处理 Android 13+ 权限 --> | <!-- 高/中/低 --> | <!-- 如 F-06 中处理 --> |
