# HomeReminder — Harness Engineering 框架描述

> 基于 [Learn Harness Engineering](https://walkinglabs.github.io/learn-harness-engineering/) 课程内容构建。
> 命名规范：`.md` 文件大写，`.json` 文件小写。

---

## 1. 核心理念

Harness = **模型权重之外的一切工程基础设施**，由五个子系统组成：

| 子系统 | 职责 | 核心文件 |
|--------|------|----------|
| **指令** | agent 入口、约束、架构决策 | `harness/ARCHITECTURE.md`, `harness/CONSTRAINTS.md`, `harness/decisions/` |
| **工具** | agent 能调用什么命令 | `tools/init.sh`, `tools/verify.sh`, `tools/clean-state.sh` |
| **环境** | 依赖、运行时版本、可重现环境 | `tools/init.sh` + 项目配置文件 |
| **状态** | 跨会话进度、功能完成情况 | `harness/feature_list.json`, `harness/PROGRESS.md`, `harness/SESSION-HANDOFF.md` |
| **反馈** | 验证命令、评审评分 | `tools/verify.sh`, `harness/EVALUATOR-RUBRIC.md` |

### 各目录定位

| 目录 | 谁写 | 谁读 | 定位 |
|------|------|------|------|
| `harness/` | 人 | 所有 agent | 知识、状态、约束 |
| `tools/` | 人 | 所有 agent | 全局工具脚本 |
| `agents/` | 人 | 对应 agent | 角色行为契约 |
| `work_templates/` | 人 | 所有 agent | work 模板，不参与归档 |
| `src_templates/` | 人 | implementer | src 模块模板 |
| `work/` | agent | agent | 当前功能的跨 agent 产出 |
| `history/` | reviewer | 人 / agent | 已完成功能归档 |
| `docs/` | 人 / planner | 人 / agent | 架构文档 + 任务拆分 |

### 关键原则

- **仓库是唯一事实来源** — agent 看不到的东西对它来说就不存在
- **WIP=1** — 一次只一个功能处于 in_progress
- **三层验证** — L1 静态（lint/type check/架构边界规则检查）→ L2 运行时（`test/unit/` + `test/integration/`）→ L3 系统级（`test/e2e/`）
- **干活和检查分开** — planner/implementer 不评估自己
- **人审先行** — feature_list 填完后，planner 先出全貌拆分，你审过再开始
- **模板驱动** — work 从 `work_templates/` 整体复制，src 模块从 `src_templates/` 复制

---

## 2. 目录结构

```
HomeReminder/
│
├── .deepseek/                           # DeepSeek TUI 专属配置
│   └── instructions.md
│
├── harness/                             # 通用 Harness 层
│   ├── ARCHITECTURE.md                  # [指令] 项目级架构总览
│   ├── CONSTRAINTS.md                   # [指令] 全局硬约束
│   ├── PROGRESS.md                      # [状态] 全局进度
│   ├── feature_list.json                # [状态] 功能清单 + 验收标准
│   ├── SESSION-HANDOFF.md               # [状态] 长会话交接
│   ├── EVALUATOR-RUBRIC.md              # [反馈] 六维度评审
│   └── decisions/                       # [指令] 架构决策记录 (ADR)
│
├── tools/                               # 项目全局工具脚本
│   ├── init.sh
│   ├── verify.sh
│   └── clean-state.sh
│
├── agents/                              # 多 Agent 角色定义（skill 格式）
│   ├── planner/SKILL.md
│   ├── implementer/SKILL.md
│   └── reviewer/SKILL.md
│
├── work_templates/                      # work 模板（不参与归档，目录结构与 work/ 一致）
│   ├── planner/
│   │   ├── BREAKDOWN.md
│   │   └── PLAN.md
│   ├── implementer/
│   │   └── .gitkeep                     # 预留，implementer 产出由 src 模块模板驱动
│   ├── reviewer/
│   │   ├── L1-REPORT.md
│   │   ├── L2-REPORT.md
│   │   ├── L3-REPORT.md
│   │   ├── FIX-QUEUE.md
│   │   └── COMPLETION.md
│   └── logs/
│       └── .gitkeep
│
├── src_templates/                       # src 模块模板（每个 src 子模块按此结构创建）
│   ├── ARCHITECTURE.md                  # 模块级架构
│   ├── CONSTRAINTS.md                   # 模块级约束
│   ├── PROGRESS.md                      # 模块级进度
│   └── code/                            # 代码文件
│       └── .gitkeep
│
├── work/                                # 当前功能 Agent 产出
│   ├── planner/
│   │   ├── BREAKDOWN.md
│   │   └── PLAN.md
│   ├── implementer/
│   │   └── .gitkeep                     # 预留
│   ├── reviewer/
│   │   ├── L1-REPORT.md
│   │   ├── L2-REPORT.md
│   │   ├── L3-REPORT.md
│   │   ├── FIX-QUEUE.md
│   │   └── COMPLETION.md
│   └── logs/
│       ├── log.json
│       └── tests/
│           └── F{id}-L{layer}-round{n}.log
│
├── history/                             # 已完成功能归档
│   └── <feature-id>-<name>/
│
├── docs/                                # 人类文档 + planner 产出
│   ├── README.md
│   ├── architecture.md
│   ├── conventions.md
│   └── task-breakdown/
│       └── overview.md
│
├── src/                                 # 源码目录
│   └── .gitkeep
├── test/                                # 三层测试目录
│   ├── unit/                            # L2 单元测试（按 src 模块平行拆分）
│   │   └── <module>/
│   │       └── .gitkeep
│   ├── integration/                     # L2 集成测试（跨模块交互）
│   │   └── .gitkeep
│   └── e2e/                             # L3 系统级端到端测试
│       └── .gitkeep
├── README.md
└── .gitignore
```

---

## 3. 整体流程

### 阶段 1：任务拆分（人审检查点）

```
你填 harness/feature_list.json
    │
    ▼
planner 读全部功能 → 生成 docs/task-breakdown/overview.md
    │
    ▼
你审阅 → OK
    │
    ▼
planner 开始 WIP=1 循环
```

### 阶段 2：WIP=1 开发循环

#### 三角色

| 角色 | Skill | 职责 |
|------|-------|------|
| planner | `agents/planner/SKILL.md` | 选功能、标记 in_progress、出方案 |
| implementer | `agents/implementer/SKILL.md` | 从 `src_templates/` 创建模块 → 写代码 → 写测试 |
| reviewer | `agents/reviewer/SKILL.md` | 三层递进验证、fix-queue、归档 |

#### implementer 模块创建

implementer 开发新模块时，从 `src_templates/` 复制到 `src/<module-name>/`，同步在 `test/unit/` 下创建 `<module-name>/` 目录，然后补全 `ARCHITECTURE.md`（模块架构）、`CONSTRAINTS.md`（模块约束）、`PROGRESS.md`（模块进度，含测试覆盖率目标），并在 `code/` 中写入代码。

```
src_templates/                    src/
├── ARCHITECTURE.md               ├── auth/
├── CONSTRAINTS.md      cp -r     │   ├── ARCHITECTURE.md
├── PROGRESS.md         ──────→   │   ├── CONSTRAINTS.md
└── code/                         │   ├── PROGRESS.md
                                  │   └── code/
                                  │       └── auth_service.dart
                                  └── storage/
                                      ├── ARCHITECTURE.md
                                      ├── CONSTRAINTS.md
                                      ├── PROGRESS.md
                                      └── code/
                                          └── db.dart
```

#### 日志规范

**操作日志** — `work/logs/log.json`（小写），JSON 行追加：

```json
{"timestamp":"2026-05-27T14:30:00","agent":"planner","action":"select","feature":"F-03","detail":"标记 in_progress"}
{"timestamp":"2026-05-27T15:00:00","agent":"implementer","action":"create_module","feature":"F-03","module":"auth"}
{"timestamp":"2026-05-27T16:20:00","agent":"reviewer","action":"verify","feature":"F-03","layer":"L1","result":"pass"}
```

**测试日志** — `work/logs/tests/F{id}-L{layer}-round{n}.log`，验证命令的完整 stdout/stderr。

#### WIP=1 循环

```
planner 选 pending → 标记 in_progress → 追加 log.json
    │
    ▼
cp -r work_templates/ work/ → 补全 planner/BREAKDOWN.md + PLAN.md
    │
    ▼
implementer 读 PLAN.md → cp -r src_templates/ src/<module>/ → 写代码 → 补全模块 ARCHITECTURE/CONSTRAINTS/PROGRESS → 追加 log.json
    │
    ▼
交给 reviewer
    │
    ▼
reviewer L1（静态分析：lint + type check + 架构边界规则检查）→ 补全 reviewer/L1-REPORT.md → 写 logs/tests/Fxx-L1-round{n}.log
    │ 不通过 → 补全 reviewer/FIX-QUEUE.md（每条包含"什么出错 + 为什么 + 怎么修"）→ 退 implementer
    │ 通过 → L2（`test/unit/` + `test/integration/` 全部通过）→ 补全 L2-REPORT.md → 写 tests/Fxx-L2-round{n}.log
    │   不通过 → 补全 FIX-QUEUE.md（每条失败附修复指引）→ 退 implementer
    │   通过 → L3（`test/e2e/` 端到端场景全部通过）→ 补全 L3-REPORT.md → 写 tests/Fxx-L3-round{n}.log
    │     不通过 → 补全 FIX-QUEUE.md（每条失败附修复指引）→ 退 implementer
    │     通过 → 补全 COMPLETION.md → 清空 FIX-QUEUE.md
    ▼
mv work/planner work/reviewer work/logs → history/<id>-<name>/
    │
    ▼
cp -r work_templates/ work/ → 重建
    │
    ▼
planner 更新 PROGRESS.md → 更新 feature_list.json（status → completed）
    │
    ▼
取下一个 pending → 循环
```

#### 关键规则

- **人审先行**：feature_list 填完后，planner 先出 overview.md，你审过再开始
- **模板驱动**：work 从 `work_templates/` 复制，src 模块从 `src_templates/` 复制
- **模板不归档**：`work_templates/` 和 `src_templates/` 永久保留
- **WIP=1**：同时只有一个功能 in_progress
- **标记先于执行**：planner 先改 status 再写方案
- **fix-queue 驱动修复**：implementer 每次启动先检查 FIX-QUEUE.md
- **每步必记日志**：写入 `log.json` 和 `tests/`
- **同功能退回 > 3 次**：记录到 PROGRESS.md 阻塞项，需人类介入

---

## 4. work_templates/ — work 模板

目录结构与 `work/` 一致，不参与归档。每个文件章节固定、内容留白（`<!-- xxx -->`）。implementer 目录为空占位。

```
work_templates/
├── planner/
│   ├── BREAKDOWN.md
│   └── PLAN.md
├── implementer/
│   └── .gitkeep
├── reviewer/
│   ├── L1-REPORT.md
│   ├── L2-REPORT.md
│   ├── L3-REPORT.md
│   ├── FIX-QUEUE.md
│   └── COMPLETION.md
└── logs/
    └── .gitkeep
```

| 模板 | 使用方 | 核心章节 |
|------|--------|---------|
| `planner/BREAKDOWN.md` | planner | 功能 ID、工作单元列表、依赖拓扑、状态表 |
| `planner/PLAN.md` | planner | 涉及模块、关键步骤、依赖、交付物 |
| `reviewer/L1-REPORT.md` | reviewer | 验证命令、输出摘要、通过/不通过、问题列表 |
| `reviewer/L2-REPORT.md` | reviewer | 测试结果、启动检查、关键路径验证 |
| `reviewer/L3-REPORT.md` | reviewer | 端到端场景、集成结果、资源清理 |
| `reviewer/FIX-QUEUE.md` | reviewer | 问题列表（层/测试名/实际结果/根因/期望行为） |
| `reviewer/COMPLETION.md` | reviewer | 功能 ID、通过层、模块完成状态表、已知限制 |

---

## 5. src_templates/ — src 模块模板

每个 `src/` 子模块按此结构创建。implementer 执行 `cp -r src_templates/ src/<module-name>/` 后补全。

```
src_templates/
├── ARCHITECTURE.md     # 模块级架构
├── CONSTRAINTS.md      # 模块级约束
├── PROGRESS.md         # 模块级进度
└── code/               # 代码文件
    └── .gitkeep
```

| 文件 | 职责 |
|------|------|
| `ARCHITECTURE.md` | 模块架构说明、对外接口、内部结构 |
| `CONSTRAINTS.md` | 模块级硬约束（禁止操作、强制规范） |
| `PROGRESS.md` | 模块开发进度（已完成/进行中/阻塞项） |
| `code/` | 模块源代码 |

---

## 6. work/ — 当前功能 Agent 产出

| 文件 | 谁写 | 谁读 | 说明 |
|------|------|------|------|
| `planner/BREAKDOWN.md` | planner | implementer, reviewer | 功能拆分 |
| `planner/PLAN.md` | planner | implementer | 实现方案 |
| `reviewer/L1-REPORT.md` | reviewer | implementer, planner | 静态分析结果 |
| `reviewer/L2-REPORT.md` | reviewer | implementer, planner | 运行时验证结果 |
| `reviewer/L3-REPORT.md` | reviewer | implementer, planner | 系统级确认结果 |
| `reviewer/FIX-QUEUE.md` | reviewer | implementer | 修复工作单 |
| `reviewer/COMPLETION.md` | reviewer | planner, 人 | 模块完成状态 |
| `logs/log.json` | 三个 agent | 人, 所有 agent | 操作日志 |
| `logs/tests/F{id}-L{layer}-round{n}.log` | reviewer | 人, implementer | 测试输出 |

---

## 7. harness/ 核心文件

| 文件 | 类型 | 职责 |
|------|------|------|
| `ARCHITECTURE.md` | 大写 .md | 项目级架构总览 |
| `CONSTRAINTS.md` | 大写 .md | 全局硬约束 |
| `PROGRESS.md` | 大写 .md | 全局进度 |
| `feature_list.json` | 小写 .json | 功能清单 + 验收标准 |
| `SESSION-HANDOFF.md` | 大写 .md | 长会话交接 |
| `EVALUATOR-RUBRIC.md` | 大写 .md | 六维度评审 |
| `decisions/` | 目录 | 架构决策记录 (ADR) |

---

## 8. docs/task-breakdown/

`overview.md` 由 planner 生成，人审阅。

---

## 9. history/ — 已完成功能归档

```
history/
└── F-03-语音录入/
    ├── planner/
    │   ├── BREAKDOWN.md
    │   └── PLAN.md
    ├── reviewer/
    │   ├── L1-REPORT.md
    │   ├── L2-REPORT.md
    │   ├── L3-REPORT.md
    │   ├── FIX-QUEUE.md
    │   └── COMPLETION.md
    └── logs/
        ├── log.json
        └── tests/
```

---

## 10. DeepSeek TUI 专属 vs 通用

| | `.deepseek/instructions.md` | `harness/ARCHITECTURE.md` |
|---|---|---|
| 谁读 | DeepSeek TUI（自动注入） | 所有 coding agent |
| 内容 | 可含 DS TUI 特有上下文 | 纯 Markdown |

---

## 11. 使用指南

1. 确定技术栈 → 填 `harness/ARCHITECTURE.md` + `harness/CONSTRAINTS.md`
2. 在 `harness/feature_list.json` 中添功能
3. 写 `docs/architecture.md` + `docs/conventions.md`
4. planner 生成 `docs/task-breakdown/overview.md` → 你审阅
5. 审过 → WIP=1 循环（`cp -r work_templates/ work/` + `cp -r src_templates/ src/<module>/`）
6. 添加项目配置 → 修改 `tools/init.sh` + `tools/verify.sh`

---

## 参考

- [OpenAI: Harness Engineering](https://openai.com/index/harness-engineering/)
- [Anthropic: Effective Harnesses for Long-Running Agents](https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents)
- [Anthropic: Harness Design for Long-Running App Development](https://www.anthropic.com/engineering/harness-design-long-running-apps)
- [Learn Harness Engineering](https://walkinglabs.github.io/learn-harness-engineering/)
