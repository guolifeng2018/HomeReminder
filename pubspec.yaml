name: home_reminder
description: "居净清单 — 纯本地离线家庭清洁事务管理应用"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  drift: ^2.22.0
  sqlite3_flutter_libs: ^0.5.24
  flutter_local_notifications: ^18.0.0
  go_router: ^14.6.2
  record: ^5.2.0
  path_provider: ^2.1.4
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  drift_dev: ^2.22.0
  build_runner: ^2.4.12
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
